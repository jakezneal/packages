/**
 * @type {import('@jakezneal/hola').HolaConfig}
 */
const config = {
    features: {
        storybook: true,
        tests: true,
    },
    defaultPath: 'playground/components',
    framework: 'vue',
};

export default config;
