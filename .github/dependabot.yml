---
version: 2
updates:
    - package-ecosystem: 'npm'
      directories:
          - /
          - /packages/*
      schedule:
          interval: 'weekly'
      open-pull-requests-limit: 10
      target-branch: main
      labels:
          - 'dependencies'
